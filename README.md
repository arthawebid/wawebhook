# Webhook Aplikasi Wa Gateway
## Clone Repositories
```bash
git clone https://github.com/arthawebid/wahook.git
```
```bash
sudo apt update

sudo apt install python3-venv -y

cd /wawebhook

mkdir data

python3 -m venv venv

venv/

source venv/bin/activate

pip install flask

pip freeze > requirements.txt

python webhook01.py
```
deactive venv
```bash
deactivate
```
